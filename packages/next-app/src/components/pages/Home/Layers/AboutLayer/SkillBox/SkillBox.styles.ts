import { css } from '@emotion/css'
import { cssBaseBox } from '../../styles'

export const root = css`
  ${cssBaseBox};
  height: 450px;
  padding: 20px;
`

export const title = css`
  font-size: 40px;
  font-weight: bold;
`
